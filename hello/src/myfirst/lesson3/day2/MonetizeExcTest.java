package myfirst.lesson3.day2;

import java.util.Locale;
// import javax.money.*;

public class MonetizeExcTest {
/*
    MonetaryAmount monetaryAmount = Money.of(new Float( 1239.63 ), euro);
    CurrencyUnit currency = monetaryAmount.getCurrency();

    NumberValue numberValue = monetaryAmount.getNumber();

    MonetaryAmountFormat eurFormat = MonetaryFormats.getAmountFormat(
            AmountFormatQueryBuilder.of(Locale.GERMANY)
                    .set(CurrencyStyle.SYMBOL)
                    .set("pattern", "#,##0.00### ¤")
                    .build());

    SimpleDateFormat enFormat = new SimpleDateFormat("MMMM", enLocale);
    SimpleDateFormat ltFormat = new SimpleDateFormat("MMMM", ltLocale);

        System.out.println("Generating invoice for " + ACCOUNT_NR);

    RuleBasedNumberFormat formatter = new RuleBasedNumberFormat(enLocale, RuleBasedNumberFormat.SPELLOUT);
    String euros =
            StringUtils.capitalize(
                    formatter.format(numberValue.getAmountFractionNumerator()));
    String cent = formatter.format(numberValue.getAmountFractionDenominator());

    final String amount = (String.format("%s %s %s %s", euros,
            monetaryAmount.getCurrency().getCurrencyCode(), cent, "ct."));

        log.info("Amount to be invoiced: " + amount);*/

}
